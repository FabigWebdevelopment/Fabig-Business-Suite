# =============================================================================
# LOCAL DEVELOPMENT - Quick Start Template
# =============================================================================
# 1. Copy this file to .env.local
# 2. Generate PAYLOAD_SECRET: openssl rand -base64 32
# 3. Start Docker: docker-compose up -d
# 4. Run migrations: pnpm db:push
# 5. Start dev server: pnpm dev
# =============================================================================

# Database (Docker PostgreSQL)
DATABASE_URI=postgresql://postgres:postgres@localhost:5432/fabig_suite_dev

# Payload CMS (generate your own!)
PAYLOAD_SECRET=your-super-secret-key-minimum-32-characters-long

# Server URL
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# OpenAI (optional for local dev)
OPENAI_API_KEY=sk-your-openai-api-key

# WhatsApp (use test credentials)
WHATSAPP_ACCESS_TOKEN=test_access_token
WHATSAPP_PHONE_NUMBER_ID=test_phone_number_id
WHATSAPP_BUSINESS_ACCOUNT_ID=test_business_account_id
WHATSAPP_WEBHOOK_VERIFY_TOKEN=fabig_suite_local_verify

# Email (optional for local dev)
RESEND_API_KEY=re_your_resend_api_key
EMAIL_FROM=dev@localhost

# SMS (optional for local dev)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Stripe (use test mode keys)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
STRIPE_PRICE_BASIC_MONTHLY=price_test_basic
STRIPE_PRICE_PROFESSIONAL_MONTHLY=price_test_professional
STRIPE_PRICE_PREMIUM_MONTHLY=price_test_premium
STRIPE_PRICE_ENTERPRISE_MONTHLY=price_test_enterprise

# Upstash Redis (optional - for Stripe subscription caching)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Background Jobs (optional)
TRIGGER_API_KEY=
TRIGGER_API_URL=https://api.trigger.dev

# Sentry (optional for local dev)
SENTRY_DSN=
